<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
        <!-- Logo -->
        <a class="navbar-brand" href="/listings">
            <i class="fa-solid fa-map-location-dot"></i> <span>Travel Nest</span>
        </a>

        <!-- Toggler -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar Content -->
        <div class="collapse navbar-collapse justify-content-center" id="navbarNavAltMarkup">
            <!-- Center Search Bar -->
            <form class="d-flex search-form mx-auto" action="/listings/search" method="GET">
                <input 
                    class="form-control me-2" 
                    type="search" 
                    name="location" 
                    placeholder="Search destinations..." 
                    aria-label="Search"
                >
                <button class="btn btn-search" type="submit">
                    <i class="fa-solid fa-magnifying-glass"></i>
                </button>
            </form>
            

            <!-- Right Links -->
            <div class="navbar-nav">
                <a class="nav-link text-dark" href="/listings/new">Add your home</a>
                <% if (!currUser) { %>
                    <a class="nav-link text-dark ms-2" href="/signup">Sign up</a>
                    <a class="nav-link text-dark ms-2" href="/login">Log in</a>
                <% } else { %>
                    <a class="nav-link text-dark ms-2" href="/logout">Log out</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>


<style>
    /* Navbar Adjustments */
    .navbar {
        height: 5rem; /* Reduced height for a more compact navbar */
        padding: 0 2rem; /* Add some padding for better spacing */
    }

    .navbar-brand {
        font-size: 1.8rem;
        color: black; /* Logo color */
        display: flex;
        align-items: center;
        font-weight: 600;
    }

    .navbar-brand i {
        font-size: 2rem;
        margin-right: 0.5rem; /* Space between icon and text */
    }

    .navbar-brand span {
        font-size: 1.6rem;
        font-weight: bold;
    }

    /* Center the search bar longitudinally */
    .navbar-collapse {
        display: flex;
        justify-content: center;  /* Centering the search bar */
        align-items: center;
        width: 100%;
    }

    /* Search form adjustments */
    .search-form {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 40%; /* Slightly larger search bar */
        margin-top: -5px;
    }

    .search-form input {
        border-radius: 20px;
        border: 2px solid black;
        padding-left: 15px; /* Added padding for better alignment */
        transition: box-shadow 0.3s ease-in-out;
    }

    .search-form input:focus {
        outline: none;
        border-color: #FF385C;
        box-shadow: 0 0 5px rgba(255, 56, 92, 0.5);
    }

    .btn-search {
        background-color: #FF385C;
        color: white;
        border-radius: 20px;
        border: none;
        padding: 0.4rem 1rem;
        transition: background-color 0.3s ease-in-out;
        margin-left: -0.7rem;
    }

    .btn-search:hover {
        background-color: #E63950;
    }

    .btn-search i {
        font-size: 1rem;
    }

    /* Right links adjustment */
    .navbar-nav {
        display: flex;
        align-items: center;
    }

    .navbar-nav .nav-link {
        font-size: 1.1rem;
        font-weight: 500;
    }

    .navbar-nav .nav-link:hover {
        color: #FF385C;
    }

    /* Space out the login/sign-up links */
    .navbar-nav .ms-2 {
        margin-left: 1rem;
    }
</style>
